# 会话管理与回收站

> 本文档描述桌面智能体客户端的**会话元数据、标题生成、会话重命名/删除与回收站**机制。核心结论：会话不再只有 `session_N.jsonl` 过程记录，新增同目录的 **`index.jsonl`** **元数据索引**（标题/创建时间/状态/项目归属，以文件名为键与会话文件关联）；运行时数据从项目内 `WorkSpace/task1/` 迁移到用户级 **`~/.aigent/projects/<slug>/.chathistory/`**（分项目隔离，对齐 Claude Code `~/.claude/projects/` 模型）；删除走**软删除两段式**（先入回收站，回收站内永久删除）；标题由**独立的小请求**在首条消息到达时与首轮对话并行生成，失败降级为首条消息截断。按「概念 → 设计 → 落地」组织。

***

## 一、概念

### 1.1 为什么需要会话元数据

改造前，会话只有过程数据 `session_N.jsonl`（每行一条消息），没有会话自身的属性：

- **列表无标题**：任务树只能显示 `session_3` 这类编号，用户无法辨识会话内容。

- **无生命周期状态**：没有"已删除"概念，要么保留要么物理删文件，误删无法挽回。

- **无时间与归属**：创建/更新时间只能靠文件 mtime 猜；未来多项目阶段无法区分会话属于哪个项目。

元数据与过程数据**分文件存放**（`index.jsonl` + `session_N.jsonl`），而不是往消息行里塞元数据：消息行是数组语义（append-only），元数据是字典语义（频繁就地更新），混放会让读写双方都别扭。

### 1.2 软删除与回收站（桌面产品惯例）

删除会话分两段：

1. **移入回收站（软删除）**：`index.jsonl` 中该会话 `status: active → trashed`，会话文件原样保留。任务树不再显示，回收站可见。
2. **永久删除（硬删除）**：在回收站中操作，物理删除 `session_N.jsonl` + 绑定的 `session_N.subagents.jsonl`（子智能体旁路记录）+ todo 文件 + 索引行。

好处：误删可还原（`restore`）；"删除"按钮永远不造成不可逆后果，破坏性操作集中到回收站页并配二次确认。

### 1.3 标题生成：独立小请求，而非解析主对话

两种候选方案：

| 方案       | 做法                                                                                      | 问题                                                  |
| -------- | --------------------------------------------------------------------------------------- | --------------------------------------------------- |
| A. 解析主对话 | 首轮回复中让模型顺带输出标题（约定格式），前端解析截取                                                             | 污染主对话历史与 system prompt；格式解析脆弱；流式渲染时标题混入正文；重试/压缩时易丢失 |
| B. 独立小请求 | 首条消息到达时，用主模型发一次**极小的非流式请求**（`max_tokens=1000, temperature=0.3`）专门生成标题，与首轮 `run_turn` 并行 | 多一次 LLM 调用（首条消息截断前 2000 字，成本可忽略）                    |

**采用方案 B**。标题生成与主对话完全解耦：不进消息历史、不占流式通道、失败可静默降级（截断首条消息前 20 字），任何异常都不影响主流程。

***

## 二、设计

### 2.1 元数据模型（index.jsonl）

与 `session_N.jsonl` 同目录，每行一条会话元数据 JSON，**以会话文件名（含前缀）为键**关联：

```json
{"num": 3, "file": "session_3.jsonl", "title": "分析 goal.py 存储机制", "title_source": "auto", "created_at": "2026-09-08T10:30:00", "updated_at": "2026-09-08T10:35:12", "status": "active", "trashed_at": null, "project": "default"}
```

| 字段                          | 类型           | 说明                                                                           |
| --------------------------- | ------------ | ---------------------------------------------------------------------------- |
| `num`                       | int          | 会话编号（展示与协议寻址用）                                                               |
| `file`                      | string       | 关联的会话文件名（**主键**，`session_` / `cron_` 前缀共用目录时避免编号冲突）                          |
| `title`                     | string\|null | 会话标题；null 时前端回退显示 `session_<num>`                                            |
| `title_source`              | string       | 标题来源：`user`（手动重命名）> `auto`（LLM 生成）> `trunc`（首条消息截断）> `none`（无标题）。低优先级不覆盖高优先级 |
| `created_at` / `updated_at` | string       | 本地时间秒级 ISO（单机桌面产品，无时区转换）                                                     |
| `status`                    | string       | `active`（任务树） / `trashed`（回收站）                                               |
| `trashed_at`                | string\|null | 移入回收站的时间（回收站列表展示"删除于"）                                                       |
| `project`                   | string       | 项目归属 slug；单项目阶段固定 `default`，项目选择阶段改为按工作目录派生（预留 `_independent` 表示独立会话）        |

写入策略：

- **原子重写**：`save_index()` 用 tmp + replace 模式（与 `save_session_history` 一致），避免半写损坏。

- **对账补录（backfill）**：`backfill_index()` 在每次 `list_sessions()` 前执行——目录里有 jsonl 但索引缺条目的补录（`title=null`、`created_at` 取文件 mtime）；索引里有但文件已不存在的剔除。老会话零成本接入，索引可随时删除重建。

- **新建即登记**：`create_new_session()` 创建会话文件的同时 `ensure_index_entry()` 写入初始条目。

### 2.2 存储位置：分项目目录（决策记录)

会话数据放哪？两个候选：

| 候选      | 路径                                        | 评估                                                                                                  |
| ------- | ----------------------------------------- | --------------------------------------------------------------------------------------------------- |
| A. 分项目  | `~/.aigent/projects/<slug>/.chathistory/` | 对齐 Claude Code `~/.claude/projects/` 模型；多项目天然隔离；项目选择阶段只需换 slug，代码零改动；会话与该项目绑定的 todo/记忆/任务等运行时数据同居一处 |
| B. 统一目录 | `~/.aigent/chathistory/`                  | 实现最简单；但会话归属关系要靠元数据 `project` 字段维护，跨项目查询/清理都是全量扫描；未来多项目时要做一次大迁移                                      |

**采用方案 A**。运行时数据（`.chathistory/.todo/.tasks/.team/.inbox/.transcripts/.task_outputs/.memory/.workflow/.scheduler`）整体从项目内 `WorkSpace/task1/` 迁到 `~/.aigent/projects/default/`，由 `paths.py` 的 `migrate_workspace_data()` 一次性完成（幂等，源不存在或目标已存在则跳过）。单项目阶段 slug 固定 `default`；项目选择阶段按工作目录派生 slug，届时只需改 `DATA_ROOT` 的计算，会话管理代码不动。

### 2.3 会话生命周期（状态机）

```
        新建（惰性：首条 chat 触发）
          │
          ▼
      ┌────────┐  trash（软删除）   ┌─────────┐
      │ active │ ────────────────► │ trashed │
      └────────┘                   └─────────┘
       ▲    ▲                          │    │
       │    └────── restore（还原）──────┘    │
       │                                     │ delete（永久）
       │  新首条消息 → 惰性新建新会话            ▼
       └────────────────────────────    物理删除
```

关键语义：

- **删除当前激活会话**：后端收到 `session_trash` 且目标恰是激活会话时，置空 agent 会话态（`session_num=None` 等），惰性会话机制下下条 chat 自动新建，前端也同步切到空态（`newSession()`）。

- **永久删除连带清理**：`delete_session_permanent()` 删除 `session_N.jsonl` + `session_N.subagents.jsonl`（子智能体旁路记录，与主文件同生共死）+ `.todo/session_N.todo.json`（todo 与会话同生共死）+ 索引行；单个失败不影响批量中其他项。

- **清空连带的旁路文件清理**：`clear_session()` 重写主 jsonl（只留系统提示词）的同时删除 `session_N.subagents.jsonl`（缓存同步失效），保证「清空会话」后不留旧子智能体记录。

- **批量删除**：`session_delete` 命令携带 `nums[]`，逐个执行，回发 `session_delete_result`（`deleted[]` / `failed[]`），前端据此 toast。**删除成功后不再全量回刷列表**：后端 `session_delete` 分支删除完成即回发 `session_delete_result`（不再调用 `_reply_sessions` 全量广播 sessions）；前端 `deleteSessions` 拿到 `deleted[]` 后，在本地 store 从 `sessions` 与 `trashSessions` 中增量移除被删会话。此举避免删除后立即「重建整张列表（每个会话逐个打开文件重数 message\_count）+ 全量广播 + React 整树重渲染」的连带开销，删除刷新即时；回收站页切换仍在挂载/ `refreshTrash` 时全量拉取，语义一致。

### 2.3.1 会话伴生文件：子智能体旁路记录（`session_N.subagents.jsonl`）

除元数据 `index.jsonl` 与会话消息 `session_N.jsonl` 之外，每个会话还可能有一个**旁路记录文件**
`session_N.subagents.jsonl`：子智能体执行过程（思考 + 工具调用 + 终态/耗时）的记录，
由 `subagent_store.SubagentStore` 维护（格式与读取规则见 03 篇 2.1.1）。

为什么单独成文件而不是塞进主 jsonl：`role=subagent` 行与标准消息混写会让 append 顺序成为隐式契约，
打断加载时的 tool 块连续性判定，进而**静默删除整轮对话并重写文件**（详见 08 篇）。
物理隔离后，压缩 / 自愈重写 / 异常恢复三类机制与子智能体记录互不影响。

生命周期与会话主文件**同生共死**：

| 操作 | 对 `session_N.subagents.jsonl` 的处理 |
| --- | --- |
| 新建会话 | 不创建（首次有子智能体时才出现） |
| 移入回收站 / 还原 | **不动**（jsonl 与旁路文件均原样保留，还原后切回即可回放） |
| 清空会话（`session_clear`） | 删除旁路文件（与消息一同清空） |
| 永久删除（`session_delete`） | 删除旁路文件 |
| 上下文压缩 / 自愈重写 | **不动**（`save_session_history` 不读也不回写旁路记录） |
| 旧数据迁移 | 首次加载时把主 jsonl 里遗留的 `role=subagent` 行抽出到旁路文件，主文件净化为纯标准消息，并留 `session_N.jsonl.bak` 快照（幂等） |

> 备份说明：`session_N.jsonl.bak` 是「迁移 / 自愈重写前」的一次性快照（谁先写谁生效，不覆盖更早的副本），
> 用于误判时人工回滚；它不参与会话列表统计（`glob("*.jsonl")` 不匹配 `.jsonl.bak`）。

### 2.4 标题生成流程

触发时机与判定（`ws_bridge.py` chat 处理中）：

```
首条 chat 到达
  ├─ fresh=true 或后端无激活会话 → 惰性新建会话（fresh_created = true）
  ├─ needs_title = fresh_created 或 当前会话历史中无真实 user 消息
  │    （排除 <system-reminder> 系统注入，判定"首轮"）
  ├─ if needs_title: _start_title_thread(...)   ← 独立 daemon 线程，先于 run_turn 提交
  ├─ await run_turn(text)          ← 主对话与标题线程完全解耦，互不等待
  └─ await _reply_sessions()       ← turn 结束统一回发（兜底，保证计数/标题最终一致）
```

- **独立线程而非协程**：标题生成是**首条消息到达即启动**的独立 daemon 线程（`_title_worker`），**先于 run\_turn 的线程提交**——标题请求最先到达 LLM 服务端。此前实现用 `asyncio.create_task` + `to_thread`，run\_turn 线程先入线程池、标题任务晚几毫秒才启动；若服务端串行排队（本地模型/单并发代理），标题请求会排在主对话流式请求之后，表现为"标题等到首轮结束才出现"。改为独立线程后彻底消除该时序缺陷。

- **独立短超时客户端**：标题请求不复用主客户端（timeout=1200s、max\_retries=3，一旦排队会悬挂整个 turn），而是新建 `OpenAI(timeout=30, max_retries=0)` 独立客户端（`TITLE_TIMEOUT=30`），只复用鉴权/地址配置——即使服务端串行，最多 30 秒后必定降级兜底，绝不悬挂。

- **max\_tokens 必须给足（推理模型兼容）**：`TITLE_MAX_TOKENS=1000`。推理模型（如 deepseek-v4-flash）的思考过程也计入 completion 预算，早期 `max_tokens=48` 会被 `reasoning_tokens` 整个吃光，content 为空或只挤出单字（实测出现过标题"写"）。

- **结果合法性校验**：清洗（去引号、空白归一、去尾部标点）后长度 < 2 视为不合法，拒绝采用，走降级兜底。

- **完成即刷新**：标题写入索引后，通过 `asyncio.run_coroutine_threadsafe(_reply_sessions(ws), loop)` 从工作线程安全调度回事件循环，立即全量回发会话列表，不等 run\_turn。

- **同会话去重**：`_title_inflight` 集合 + 锁防止同一会话并发多个标题线程（clear 后重发首条消息等场景）。

- **索引并发写保护**：`SessionManager._update_entry`（读-改-写 JSONL 索引）整体置于 `_index_lock` 互斥锁内——标题线程与用户改名/回收站操作并发更新索引时不会交错丢更新。

- **Prompt 约束**：`TITLE_SYSTEM_PROMPT` 要求"不超过 16 字、直接输出标题、无引号句号解释"；结果再经清洗（去引号、空白归一）并截断 24 字。

- **降级与保护**：LLM 请求失败/为空/不合法（超时 30s、单字标题等）时降级为 `_fallback_title()`——按标点切分取首个语义片段（≤16 字，如"帮我写一个简单的python程序，越简单越好…"→"帮我写一个简单的python程序"），而非盲目截断；`set_auto_title()` 检查 `title_source == "user"` 则跳过——即使标题线程与用户的手动重命名并发，也永不覆盖用户标题；线程内任何异常静默吞掉，绝不影响主对话。

- **手动重命名**：`rename_session()` 写入 `title_source=user`，截断 60 字，空标题报错。

### 2.5 协议扩展

在 03 篇双层消息模型上新增（`kind` 均走统一信封）：

| kind                    | 方向    | 载荷                                 | 说明                                                                                                      |
| ----------------------- | ----- | ---------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `session_rename`        | 前端→后端 | `num` / `title`                    | 重命名（title\_source=user）                                                                                 |
| `session_trash`         | 前端→后端 | `num`                              | 软删除，移入回收站                                                                                               |
| `session_restore`       | 前端→后端 | `num`                              | 从回收站还原                                                                                                  |
| `session_delete`        | 前端→后端 | `nums[]`                           | 批量永久删除                                                                                                  |
| `trash_list`            | 前端→后端 | —                                  | 拉取回收站列表                                                                                                 |
| `sessions_trashed`      | 后端→前端 | `sessions[]`                       | 回收站列表（trash\_list 的响应，字段同 sessions）                                                                     |
| `session_delete_result` | 后端→前端 | `deleted[]` / `failed[]`           | 批量删除结果（前端 toast）                                                                                        |
| `session_model`         | 前端→后端 | `num` / `model_id?` / `overrides?` | 持久化会话绑定模型与参数（`overrides` 存按模型 id 的 UI 档位 map `{[model_id]: {thinking_strength?, max_context_option?}}`） |

`sessions` 事件载荷升级为元数据 dict（`num/title/title_source/status/created_at/updated_at/trashed_at/message_count/file`），不再是裸编号列表；`sessions_list` 的响应与所有会话变更后的全量回发共用此结构。每会话绑定模型 id 存于 `model_id`，参数存于 `overrides`（**按模型 id 分别保存**的 map，每个模型独立存取、互不串改）；由 `session_model` 写入（新建会话由前端收到 `session` 信封后写入；首条 `chat` 后端只落 `model_id`）。

**每会话独立模型绑定**：`SessionRuntime` 构造该会话 `Agent` 或其模型配置重载时，先 `snapshot_llm_env()` 快照全局 LLM 环境变量，加 `ENV_LLM_LOCK` 后按其元数据 `model_id` 用 `apply_model_to_env()` 换绑 env，再构造 / `reload_llm_bindings()`，随后 `restore_llm_env()` 还原全局 env（详见 `agents/session_runtime.py`）。实现「每个会话真正跑在各自模型上」，切换 / 新建（继承上一会话）互不干扰。

**首建必须在换绑 env 之后、于锁内构造 Agent**（`_bind_agent_env` 的 rebuild=False 分支接收 **Agent 工厂**而非已构造实例）：`Agent.__init__` 一次性捕获模型/base\_url/api\_key，若把 `Agent(silent=True)` 作为参数值传入（参数求值先于函数体换绑 env），首建会捕获**全局默认模型**，`_bound_model` 却记录会话 meta model\_id，此后切换模型若恰好切回原 meta 值，比较失效 → 表现为「切换模型不生效」。修复后首轮即按会话绑定模型运行，后续模型切换经 `_bound_model` 比对正常触发换绑。

### 2.6 前端交互设计

**任务树（TaskTree）**：

- **标题展示**：`sessionDisplayName(s)` = `title ?? "session_<num>"`；右侧保留消息计数 badge。

- **弹出菜单（SessionMenu）**：会话行 hover 显示 `⋯` 三点按钮；点击与**右键菜单**（`onContextMenu`）共用同一个 `SessionMenu` 组件（fixed 定位于触发坐标，靠近视口边缘回收；点外部/Esc 关闭）。菜单项：`重命名`（edit 图标）、`删除`（trash 图标，danger 红色）。流式进行中（`isSending`）菜单项禁用。样式：容器背景 `--color-bg-main`（纯白、完全不透明，带边框与阴影，悬浮于内容之上，不透出下层内容）。

- **行内重命名**：菜单点"重命名" → 会话行原位变输入框（`autoFocus`，初值为现有标题），`Enter` 提交、`Esc` 取消、`blur` 提交；空值或与现显示名相同不提交。提交走 `session_rename`，成功后 `sessions` 事件回刷列表。

- **删除**：菜单点"删除" → `session_trash` 软删除；若删的是当前激活会话，前端同步切到空态；toast「已移入回收站」。

**回收站页（设置弹窗 → 左侧菜单「回收站」）**：

- 入口：设置弹窗左侧菜单 `通用 / 模型 / 回收站 / 关于`；切到该页时拉取 `trash_list`。

- 列表行：复选框 + 标题（`sessionDisplayName` 回退）+ 元信息（`N 条 · 删除于 MM-DD HH:mm`）+ `还原` 按钮 + `删除` 按钮。

- **单选/多选/全选**：行复选框切换；顶部「全选」label 切换全部，选中时显示「已选 N 项」。

- **批量彻底删除**：顶部「彻底删除(N)」按钮，选中为空时禁用。

- **二次确认（arm 模式）**：首次点击进入确认态（按钮文字变「确认删除 N 项？」/单行变「确认」并加红色 `confirm` 类），**3 秒内**再点执行、超时自动恢复——不弹系统对话框，交互更轻。

- **还原**：行内「还原」→ `session_restore` → 刷新回收站与任务树两个列表。

***

## 三、落地

### 3.1 后端文件与职责

| 文件                              | 变更                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `agents/paths.py`               | 新增 `PROJECTS_ROOT` / `DEFAULT_PROJECT_SLUG` / `INDEPENDENT_PROJECT_SLUG`（预留）/ `DATA_ROOT` 常量；全部运行时目录改挂 `DATA_ROOT`；新增 `migrate_workspace_data()` 一次性迁移（`ensure_dirs()` 导入期调用，幂等）                                                                                                                                                                                                                                                                                                                                                           |
| `agents/session_manage.py`      | 新增元数据区段：`index_file` / `load_index()` / `save_index()` / `backfill_index()` / `ensure_index_entry()` / `_update_entry()`（加载→定位→mutate→刷 updated\_at→原子写回的公共底座）/ `rename_session()` / `set_auto_title()` / `trash_session()` / `restore_session()` / `delete_session_permanent()`；`create_new_session()` 同步登记索引；`list_sessions(status)` 改返回元数据 dict（`status="active"` 任务树 / `"trashed"` 回收站）                                                                                                                                                |
| `agents/ws_bridge.py`           | 新增 `TITLE_SYSTEM_PROMPT` + `_generate_session_title()`（独立短超时小请求，`TITLE_TIMEOUT=30`、`TITLE_MAX_TOKENS=1000`、独立 OpenAI 客户端）+ `_fallback_title()`（标点切分兜底）+ `_title_worker()` / `_start_title_thread()`（首条消息到达即启动的独立 daemon 线程，先于 run\_turn 提交，完成后经 `run_coroutine_threadsafe` 回发列表）+ `_has_real_user_turn()`（首轮判定，排除 system-reminder 注入）；chat 处理中首条消息到达即并行生成/降级标题；新增 `session_rename` / `session_trash` / `session_restore` / `session_delete` / `trash_list` 五个命令处理器；`chat` 改为按 `num` 派发到对应 `SessionRuntime`（缺省惰性领号）、新增 `stop` 命令（只停指定会话那一轮） |
| `agents/session_runtime.py`（新增） | `SessionRuntime`：每会话一个独立 `Agent` 实例 + 会话级事件路由（`deliver`）+ 独立停止信号 + `busy` 同会话并发锁；`SessionRuntimeRegistry.get_or_create/remove/is_busy/get/request_stop`：`start_turn` 在后台线程跑 `run_turn`（事件循环不阻塞），开始/结束经 `_deliver` 下发 `session_status`；`session_clear`/`session_trash` 对运行中会话拒操作并回 `error`；`agent_full_v2.py` 增强 `request_stop()`/`agent_loop` 协作式收尾、`streaming_client.py` 增加 `TurnStopped`                                                                                                                                                 |

### 3.2 前端文件与职责

| 文件                                                          | 变更                                                                                                                                                                                                                                                                                                                                                                                             |
| ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `protocols/agentProtocol.ts`                                | `SessionMeta` 扩展元数据字段；`ControlKind` 增加五个新命令；`UiEvent` 增加 `sessions_trashed` / `session_delete_result`；`AgentEvent` 增 `session_num`（事件路由）、`UiEvent` 增 `session_status`、`ControlKind` 增 `stop`、`ChatPayload` 增 `num`                                                                                                                                                                             |
| `store/agentStore.ts`                                       | 新增 `trashSessions` 状态、`SettingsTab` 增加 `'trash'`；`sessionDisplayName()` 展示名回退工具函数；`refreshTrash` / `renameSession` / `trashSession` / `restoreSession` / `deleteSessions` actions；`handleEvent` 处理新事件；**多会话状态模型**：`messagesBySession` 每会话独立缓冲 + `activeSession` 投影 + `runningSessions`（运行指示/停止按钮）+ `completedBg`（后台完成绿点未读）；`send` 带会话号、新增 `stop` action（只停当前显示会话一轮）、`switchSession` 仅切换投影不打断执行 |
| `components/Sidebar/TaskTree.tsx`                           | 标题展示、三点/右键菜单、行内重命名（见 2.6）；会话状态点：运行中青色脉冲（`running`）与后台完成绿点（`done`）                                                                                                                                                                                                                                                                                                                              |
| `components/Sidebar/SessionMenu.tsx`（新增）                    | 弹出菜单组件：外部点击/Esc 关闭、边缘回收、danger 删除项                                                                                                                                                                                                                                                                                                                                                             |
| `components/Settings/TrashSettings.tsx`（新增）                 | 回收站页：选择集、全选、arm 二次确认（3 秒定时器）、还原/批量删除                                                                                                                                                                                                                                                                                                                                                           |
| `components/SettingsModal.tsx`                              | 左侧菜单加入「回收站」，切页拉取列表                                                                                                                                                                                                                                                                                                                                                                             |
| `components/Chat/InputBox.tsx`                              | 发送按钮在 `isSending` 时切换为 `■ 停止`（点击只停当前显示会话一轮）                                                                                                                                                                                                                                                                                                                                                    |
| `main/index.ts` + `preload/index.ts` + `preload/index.d.ts` | IPC 通道 `agent:renameSession` / `agent:trashSession` / `agent:restoreSession` / `agent:deleteSessions` / `agent:listTrash` / `agent:stop`；`agent:send` 增 `num` 透传，映射到新 WS 命令                                                                                                                                                                                                                    |
| `styles/sidebar.css`                                        | 会话菜单、行内重命名输入框、tree-more 按钮样式；`tree-dot` 状态点（running/done）                                                                                                                                                                                                                                                                                                                                      |
| `styles/settings.css`                                       | 回收站页（trash-page/header/list/row、confirm 态按钮）样式                                                                                                                                                                                                                                                                                                                                                 |
| `components/common/Icon.tsx`                                | 新增 `more` / `restore` / `edit` 图标                                                                                                                                                                                                                                                                                                                                                              |

### 3.3 取舍与边界记录

- **为什么用 jsonl 存索引而不是 json**：与会话文件格式统一，追加/重写共用一套 JSONL 读写习惯；坏行跳过的容错策略也一致。

- **为什么以文件名为键而不是 num**：`session_` 与 `cron_` 前缀共用 `.chathistory` 目录时，纯 num 会在索引里撞键；文件名天然唯一。

- **索引为什么全量重写而非就地改行**：jsonl 不支持原地修改，重写用 tmp+replace 原子替换；会话量级（几百）下全量重写开销可忽略。

- **标题长度**：LLM 生成截 24 字（prompt 约束 16 字 + 余量）；手动重命名截 60 字。

- **首轮判定排除系统注入**：`<system-reminder>` 开头的 user 消息（todo reminder 等）不算真实用户消息，避免"切到老会话发首条消息"误触发标题生成，也避免新会话被 reminder 抢了标题。

- **回收站不自动清理**：不做"30 天自动清空"，永久删除一律用户手动（单机产品，磁盘压力可控，语义最简）。

- **`_ensure_session_manager()`**：惰性会话下 `agent.session_manager` 可能为 None（尚未 init/switch），列表/重命名等只读操作前先兜底构建，构建后 `init_session` 复用同一实例。

- **并发执行的设计**：每个会话一个独立 `SessionRuntime`（各自 `Agent`/缓冲/停止信号），克服"单会话串行会阻塞切换"的瓶颈；`chat` 只派发不等待，事件循环保持可读。每条流式事件携带 `session_num`，前端按号路由到对应缓冲，后台会话增量各自累积——这正是"切换不断流、多会话同跑可来回查看"的根基。

- **绿点（未读）语义**：后台会话执行完成（当前显示的不是它）时前端把它计入 `completedBg` 亮绿点，切到该会话即清除。**只标记"完成时刻非当前显示"** 这一种未读，避免引入复杂会话级已读表。

- **停止语义**：`stop` 只停 `num` 指定会话正在执行的那一轮（会话级事件置位协作式收尾），其它后台会话不受影响；同会话并发 turn 被拒，防止两线程同写一个 jsonl。运行中会话不可 `clear`/`trash`（先停后删）。

### 3.4 验证要点

1. **后端**：`SessionManager` 单测思路——`create_new_session` 后 `index.jsonl` 有条目；`trash` 后 `list_sessions("trashed")` 可见、`list_sessions()` 不可见；`restore` 回到 active；`delete_session_permanent` 后 jsonl/todo/索引行三者皆无；`backfill_index` 对老目录（无索引）能补录、对幽灵索引行能剔除；`set_auto_title` 不覆盖 `title_source=user` 的条目。
2. **迁移**：在含旧 `WorkSpace/task1/.chathistory` 的环境启动一次 → 数据搬到 `~/.aigent/projects/default/`，再次启动不重复搬（幂等）。
3. **前端**（`frontend` 下 `npm run dev`）：新会话发首条消息 → 列表出现自动标题；`⋯`/右键 → 重命名/删除；删除后任务树消失、设置→回收站可见 → 还原/彻底删除（二次确认）；删当前激活会话 → 主面板回空态。

> 返回：[00-README.md](00-README.md) · 上一篇：[06-设置与模型管理.md](06-设置与模型管理.md)

