# 桌面端前端设计文档（目录导航）

本目录存放 **TraeWork 复刻桌面端（Electron + TypeScript）** 的完整设计与开发文档。
后端是已有的 Python Agent（OpenAI SDK 流式改造版），前端作为其 UI 壳。

> 阅读建议：完全零基础 TS / Electron 的读者，请从 `01-技术框架设计.md` 开头的「前置概念」读起，
> 每个文档都按「概念 → 设计 → 落地」的顺序组织，配套学习资源在文末给出。

***

## 文档清单与阅读顺序

| 顺序 | 文档                 | 内容                                                      | 读者目标           |
| -- | ------------------ | ------------------------------------------------------- | -------------- |
| 1  | `01-技术框架设计.md`     | 整体架构：Electron 进程模型、技术选型、目录结构、前后端边界、数据流总览                | 建立全局认知         |
| 2  | `02-界面功能设计.md`     | TraeWork 界面复刻：三栏布局、侧边栏、聊天面板、输入框、设计系统（色板/字体/间距）          | 明确「要画什么」       |
| 3  | `03-前后端通信协议.md`    | WSSink 对接：事件线协议、控制命令、会话/目标/待办交互；历史回放与**系统注入消息约定**（`<system-reminder>` 过滤规则、记忆索引 / 环境 / 工作区指令的按需注入 §2.1.2） | 明确「数据怎么来」      |
| 4  | `04-开发路线图与后续增量.md` | 分阶段开发计划：P0 壳 → P1 骨架 → P2 联调 → 后续增量（模型/历史管理等）           | 明确「怎么一步步做」     |
| 5  | `05-配置与数据目录管理.md`  | `~/.aigent` 用户级配置目录：目录结构、分层优先级、启动加载流程、密钥管理、迁移行为、分项目运行时数据（`projects/<slug>/`） | 明确「配置放哪、怎么加载」  |
| 6  | `06-设置与模型管理.md`    | 设置弹窗（居中左侧菜单：通用/模型/归档/关于）、模型管理页（左「模型服务」列表 + 右连接详情）、`llmconfig.json` v2 连接→模型两级配置、远端模型列表刷新、自定义供应商/模型、`providers.json` 预置目录与热切换 | 明确「设置与模型配置怎么做」 |
| 7  | `07-会话管理与回收站.md`  | 会话元数据（独立 `meta` 文件 + `index.jsonl` 兜底，含 `work_root` 沙箱根快照）、**会话短 id 命名与最近使用排序**、标题自动生成（简单两段式：前 30 字默认 + 首轮后 LLM 精炼 ≤20 字）、会话行**悬停信息卡**（完整标题/所属项目/token 消耗/缓存命中率/最后更新）、重命名/**归档**（2026-09-20 由「删除」更名，机制不变）/还原、归档页（设置弹窗内；带工作空间筛选，默认「默认工作空间」）             | 明确「会话生命周期怎么管」   |
| 8  | `08-子智能体执行过程持久化与回放改造方案.md` | 子智能体流式展示与历史回显：现状缺陷证据（含可复现的数据丢失）、旁路记录文件方案、文件级改动清单与验收标准（**已实施**，文末含实施记录） | 明确「子智能体过程怎么存、怎么回放」 |
| 9  | `09-日志系统.md`   | 前后端统一日志：`~/.aigent/logs` 按日期分文件、后端 `agents/logger.py`（DailyFileHandler + 崩溃兜底）、前端 `logger.ts`、全量埋点清单 | 明确「运行时怎么留痕、怎么排障」       |
| 10 | `10-Task任务系统改造方案.md` | **todo 下线 + 会话内任务面板**：工具面/提示词改动清单、`Task` 数据结构（层级/分组/派生 blocked）、**存储布局**（§16：每会话一 JSON `.tasks/<scope>.json`、文件内以组为 key、`group_id` 不落任务体、原子写 + 路径级锁）、`task_board` 幂等快照推送、面板交互（固定高度/折叠/执行完前不可关闭；「继续执行」按钮 2026-09-16 已删除，续跑改由输入框手动发消息）、会话删除级联清理、**中断续跑设计**（提醒注入 + in_progress 归一）（**已实施**，文末含两轮实施记录） | 明确「任务怎么存、怎么显示、中断后怎么接着干」 |
| 11 | `11-工作空间管理.md` | **多工作空间（多项目）**：工作空间的组成（真实目录 = 工具沙箱根 + `~/.aigent/projects/<id>/` 元数据目录）、`projects.json` 索引与 `ws` 短码、隔离/共享清单（记忆按空间、技能全局）、路径束 `WorkspacePaths` 与「不传 workspace = default」铁律、**会话 id 跨空间唯一**、5 个新控制命令与 `projects` 信封、侧边栏工作空间树 + 输入框 chip 选择器、删除的二次确认与守卫、失败态（**已实施**） | 明确「多个项目怎么隔离、会话归属怎么解析」 |
| 12 | `12-附件与文件输入.md` | **会话附件（添加文件或图片）**：两类表示（磁盘**账本**只存中性引用块 / 发送边界**现算**厂商线格式）、三类附件、**统一转换层 `doc_convert`**（PDF → Markdown + 页图资产，按"文本层是否够密"决定渲不渲染每页，实测请求体余量 60%）、**图像主通道**（页图按 `<!--img:pN-->` 锚点交错进请求体，`detail` 档位，text-only 模型降级为占位）、**chip 四态**（staging/ready/**degraded 琥珀**/failed，副标题显示 `12 页 · 5 图 · 3 表`，不再用"已提取 N 字"当解析成功的证据）、`.attachments/` 存储布局与草稿 TTL/孤儿 GC/级联清理、`attachment_stage` 命令与 `attachments_staged` 信封、`aigent-att://` 缩略图协议与三层路径安全、降级矩阵、「仅附件无正文」的发送语义、**L1 裁剪保住附件消息**、实测验证数字（**已实施**） | 明确「文件怎么交给模型、历史怎么复现」 |
| 13 | `13-引用文件与文件夹（@-mention）.md` | **引用文件/文件夹（@-mention）**：与附件的**根本分野**（附件=搬进上下文，引用=指一条路；零复制零存储）、后端叶子模块 `agents/refs.py`（BFS 扁平列目录 + 忽略清单 + 3000 上限 + 跳 symlink）、`refs_list` 命令与 `refs` 信封（default 空间回 `disabled` 而非 `error`）、中性引用块 `{"type":"ref"}` 与**发送边界合并成一个说明文本块**（提示词约束写在注入块里，故系统提示词快照无需更新）、**输入区从 textarea 换成 Tiptap 富文本**（Mention 胶囊 NodeView、`@` Suggestion、面板向上弹出且条目**每次渲染现算**、`editorProps.handleKeyDown` 先于插件执行的按键顺序）、`serializeDoc` 序列化契约（`text` 恒 str + `refs[]`）、五处发送守卫统一到 `hasSendableContent`、改造后重验的输入区硬约束、**气泡里只显示胶囊**（`@相对路径` token 就地还原成胶囊，裸路径不再出现；RefBar 退化为兜底行；输入区与气泡两组胶囊共用浅蓝配色变量）、实测验证数字（**已实施**） | 明确「工作空间里的文件怎么引用给模型、输入区怎么支持 token 胶囊」 |
| 14 | `14-工具读图（view_image）.md` | **工具读图（`view_image`）**：模型按需把工作空间内的图片读进上下文（**工具内部不调用模型、不做文字转述**——"返回值形状就是模型能看到的东西"）、与附件/引用三分工（触发者/存储/位置/何时进上下文/块类型/谁付 token）、中性块 `{"type":"tool_image"}` 与**合成 user 消息**（Chat Completions 的 tool 消息塞不进图片 → 跨供应商支持矩阵、**图片消息必须在该批 tool 消息之后追加**的协议硬约束）、不落盘的**内存缩放**、单轮图片上限、三态能力门控与 `history_has_images` 短路、`run_read` 命中图片时指路、回放按 marker 跳过、取舍（为什么不做预内联 / 不用视觉子模型转述 / 不切 Responses API）（**已实施**） | 明确「模型怎么自己看图、像素怎么进请求体、为什么不能用 tool 消息带图」 |
| 15 | `15-统一文件读取（run_read 按类型分派）.md` | **读文件收敛为唯一入口 `run_read`**（对齐 Claude Code 的"一个 Read 工具"）：按类型分派（文本 / 图片 / PDF=文本层+页图 / Office=文本+表格结构，**魔数优先**）、**多图工具结果块**（一个结果可带 N 张页图，归一化兼容存量单数字段）、图片预算**按"组"计、绝不切页**、页图缓存落在 `<workdir>/.aigent/pages/<key>/`（`@` 忽略清单 + 目录内 `.gitignore` + TTL 清剪；**不可写则降级纯文本，正文永不因页图而丢**）、Office 抽取上移统一转换层（`converter=office_text` + "丢了什么"如实声明）、子智能体与队友路径的图片就地展开、退役 `run_read_pdf` / `view_image` 的完整波及面与 L0 快照代价（**已实施**） | 明确「模型怎么读一个文件、复合内容文档（图表/扫描件/Office）怎么真正进上下文」 |
| 16 | `16-对话中的用户确认选项（ask_user）.md` | **用户确认选项（`ask_user`）**：模型在同一回合内向用户提问并**阻塞等待**作答。**为什么是工具调用而不是"特殊正文块"**（范式 A vs B 的六项对照：schema 校验/流式聚合/配对/落盘/回放/压缩兼容，以及"答案回填是 tool_result 而非新 user 消息"的根本差别）、跨线程请求-应答 broker（`asyncio.to_thread` 让阻塞工作线程不卡事件循环；**等待期绝不持锁**）、`announced` 闸门保证「ask_request 先于 ask_resolved」、四桶分桶（ask 独占串行）、前端两块 UI（**待确认面板**一题一屏，出现时输入区**整块让位** / **只读小结块**插在正文里发起提问那一刻的位置，2026-09-22 两处交互调整）、等待期 chat 消息仍被拦截为自由作答（后端通路保留，前端不再暴露该入口）、`result_text` 作为唯一展示载体（前端零解析，实时=回放）、12 条竞态与边界（停止/删会话/断线重连/孤儿 tool_call/幂等丢弃）（**已实施**） | 明确「模型怎么问用户、答案怎么回到同一轮、面板与小结块怎么分工」 |
| 17 | `17-权限管控（两档模式与审批流）.md` | **权限管控**：从终端 `input()` 三闸门升级为完整权限体系——**两档模式（默认/完全访问，对齐 WorkBuddy）** + **前端审批卡片（范式 C，对齐 Claude Code/Codex，决定不进模型上下文）**。八步制式判定优先级链（**硬拒绝（含敏感路径与 bash 裸路径扫描）任何模式不可越** → 完全访问 → 预授权目录 → ~~自定义允许（2026-09-22 下线）~~ → 会话内允许 → 类别规则 → 审批；**⑦ 内「危险模式先于白名单」次序修正**）、bash 命令**分段归一化 + token 边界匹配**（堵双空格/管道掩护绕过，修复 `write_file`/`edit_file` 工具名失效 bug 与两份黑名单不同步）、`safe_path` 从一刀切改「**有效目录集**（工作区 ∪ 全局/空间额外目录 ∪ 会话批准目录）」+ **额外目录三级授权**（全局 permissions.json / 空间 projects.json(P2) / 会话 session_allows，输入区目录 chip 主动授权与撤销，对齐 additionalDirectories / `/add-dir`；审批弹窗记目标**父目录**收敛授权面）、`ApprovalBroker`（克隆 interaction.py 线程模型：超时自结算/停止/重连重放/幂等作答）、审批决定落 tool 行 `approval` 元数据（天然不进模型，回放恢复徽标）、模式存取继承链（会话 meta ← 工作空间最后更改 ← permissions.json 兜底）、输入区盾牌 chip、设置中心**权限管理页**（**七分区**：额外目录/安全白名单/危险/硬拒绝/MCP 策略/超时 + 页首「判定顺序」区，由后端下发）、子智能体/后台/cron/CLI 各通道行为矩阵、13 条竞态边界、文件级改动清单与 P0-P2 批次、P0 实施记录（两族常量契约收敛、回放链路补通、69 测试）、**§8 P1 管理面收口**（设置中心权限管理页落地 + 揪出「`permission_config` 回包整份覆盖 store 导致保存后整页塌成加载态」的前端契约缺陷，91 测试 + 51 条运行时断言）、**§9 二次改造**（删 ⑦ 自定义规则使八分区→七分区、⑦ 内「危险先于白名单」次序修正顺带修掉 `cat x > /etc/passwd` 被白名单掩护放行的安全洞、`deny_patterns` 跨段模式补 `cross_pattern_matches`、存量 `rules` 迁移 + `rules-bak` 备份，98 测试 + 21 条运行时断言）（**P0 与 P1 管理面已实施**，文末含实施记录） | 明确「权限怎么判、审批怎么弹怎么记、模式怎么存怎么继承、规则怎么配」 |
| 18 | `18-权限管理设置页开发方案.md` | **权限管理设置页（P1 补做）**：17 篇只有 §5.3 设计表、代码 0 行的部分单独拆成可开工方案——**缺口盘点**（逐项实测证据）、两条命令契约（`permission_config_get/save`，内置清单由后端 `builtin_snapshot()` 下发而非前端硬编码）、**生效语义矩阵**（只有 `additional_dirs` 需补在途会话刷新，其余靠 mtime 热加载；`_extra_dirs` 共享 set 的原地更新红线）、归一化与 warnings（绝对路径校验/去重/白名单空回落）、前端四层链路与八分区控件表、草稿保存语义、zustand selector 约束、踩坑清单 **11 条**（第 11 条 = 落地时真踩到的「回包整份替换 store → 保存后整页塌回加载态」，回包是补丁不是快照，消费端必须 merge）、三批任务拆分与验收标准（**已实施**，文末 §7.1 含落地情况与两处偏离；**同日二次改造**见 §7.2：删 ⑦ 自定义规则、八分区→七分区、判定次序上界面，§2.1/§3.2 为改造前原貌） | 明确「设置页怎么读怎么写、保存后怎么立即生效」 |
| 19 | `19-右侧面板（文件与变更）.md` | **右侧面板（多标签工作区）**：从「右栏默认隐藏」推进成可用的按会话隔离的标签工作区，本期交付**文件**（目录树 + 预览）与**变更**（git 四分组 + 内联 diff）两个视图，终端/浏览器占位待第二期。核心是**按会话隔离**（状态载体选**会话元数据** `right_panel` 而非 `localStorage`，与 `unread`/`permission_mode` 同源，白拿跨重启与多窗口一致）、**单栏混放标签**（视图标签与文件标签同排）、**预览位 vs 常驻位**（会话内点文件链接落唯一预览位、就地顶替不跳位、斜体 + 来源圆点；树/变更落常驻位、上限 12）；新增 4 个控制命令（`file_read`/`git_status`/`git_diff`/`session_ui`，前者三个点对点回执、`session_ui` fire-and-forget 且不进主进程 `pending` 表）与 `git_changes.py` 叶子模块（porcelain `-z` 解析含 rename 双字段坑、恒加 `core.quotePath=false` 保中文）；前端两级内存桶（`layoutBySession` 长期 / `liveBySession` 只留当前会话）+ 五条 merge 纪律 + 防抖落盘；**开关落点三次调整**（同日：两枚带字按钮 → 一枚图标 → 上移到**窗口自绘标题栏** `.titlebar`，与窗口标题同层，聊天区顶栏随之删除；原生标题栏隐去靠主进程 `titleBarStyle:'hidden'` + `trafficLightPosition`；第三次修"点得开、点不关"—— 取反改为读 `get()` 的 `togglePanelOpen`，拖拽区由"整层 drag"改为三块 `.titlebar-drag` 填充块，**不变式：没有任何 drag 矩形与开关相交**）；；第四次修「文件标签跳到终端右侧」—— 新文件标签不再一律追加栏尾，改为插在**最后一个文件相关标签之后**（`fileTabAnchor`，树来源与会话预览位同规则，已有标签一枚不动）；实测 **139 后端测试 + 157 条交互断言 + 12 条真实 Electron 拖拽/点击断言全 PASS**（无 A/B 对照，改用反证型断言）（**已实施**） | 明确「右栏怎么按会话隔离、标签怎么放、文件与变更怎么显示」 |
| 20 | `20-沙盒（执行隔离）.md` | **沙盒（执行隔离）**：与 17 篇权限管控的**分层关系**（策略层"允不允许" vs 执行层"实际能碰什么"，互补不可互替）、**覆盖范围论证**（bash 是唯一子进程/网络风险面；文件工具已有 safe_path 硬墙）、**用户可编辑的持久化模板 + 占位符**（`~/.aigent/sandbox/seatbelt.sb` / `bwrap_args.txt`，首次落盘幂等不覆盖用户文件；`{{WORKDIR}}/{{EXTRA_WRITABLE}}/{{TMPDIR}}/{{HOME}}/{{COMMAND}}` 按会话动态替换，路径统一 `resolve()` 防符号链接坑；必需占位符保存校验拒存 + 执行前体检兜底）、**平台矩阵与后端抽象**（macOS Seatbelt `sandbox-exec` / Linux bubblewrap `bwrap` 试跑探测 / Windows 诚实降级裸跑 + 每进程一次 warning；allow-by-default + 定向 deny 模板，含 `/dev/null` 设备回环）、配置项与**热生效**（`SANDBOX_ENABLED` 每次读 env + 设置页保存后直接覆写 env，勿靠 config.load() 的 setdefault）、`sandbox_config_get/save` 点对点信封（字段部分更新 + reset 恢复默认）、设置弹窗**沙盒 tab**（开关即改即存 + 双平台分区编辑器，当前平台高亮）、与权限管控互不改动（full_access 免审批不免墙；沙盒拦截是执行期失败走 run_bash 错误契约）、实施记录两个实战坑（`/var→/private/var` 符号链接使 Seatbelt subpath 匹配落空；`(deny file-write*)` 误伤 `/dev/null` 导致 git 全挂）（**已实施**，160 后端测试 + 前端构建通过） | 明确「沙盒和权限管控什么关系、命令怎么被 OS 级隔离、策略模板怎么编辑怎么生效」 |

***

## 核心结论（速览）

- **技术栈**：Electron + TypeScript + React，构建用 Vite，样式用 CSS（模块化）。后端 Python 以**子进程**方式由 Electron 拉起，通过 WebSocket 通信。

- **前端边界**：前端只负责「渲染 + 交互 + 把用户意图转发给后端」，所有 Agent 逻辑、工具执行、流式生成都在 Python 后端。

- **数据通道**：后端 `WSSink` 已能产出 JSON 行协议事件（thinking / content / tool\_call…），前端按行解析还原成消息流。

- **复刻范围**：初期严格复刻截图所示——左侧边栏（快捷操作 / 任务树 / 用户信息）+ 中央聊天面板（空态品牌 + 多行输入框 + 工具栏 + 快捷命令 chips）。

- **后续增量**：模型管理、对话历史管理、插件市场、自动化、认证等，后端暂无对应接口，列为独立增量任务。

***

## 术语对照

| 后端（Python）       | 前端（解释）                       |
| ---------------- | ---------------------------- |
| `Agent`          | 会话智能体实例，前端通过协议与其对话           |
| `StreamEvent`    | 一条流式增量，前端逐条渲染                |
| `thinking_delta` | 模型思考过程的增量片段（灰色）              |
| `content_delta`  | 正式回复内容的增量片段                  |
| `tool_call_*`    | 工具调用（未来前端可视化工具条）             |
| `session`        | 一组对话历史（复用后端 session\_number） |
| `WSSink`         | 后端把事件序列化推给前端的通道              |

> 下一篇：[01-技术框架设计.md](01-技术框架设计.md)

