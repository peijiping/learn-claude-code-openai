# 04 - 开发路线图与后续增量

按「先跑通核心对话 → 再补界面细节 → 最后扩展后端缺的能力」的节奏推进。
目标：**用尽量少的改动，先让"输入 → 流式打字机 → 工具折叠条"这条主链路跑起来**，
因为这是后端当前唯一能稳定提供的价值。

---

## P0 — 工程骨架启动（壳）

**目标**：能打开一个 Electron 空窗口。
- 用 `npm create @quick-start/electron` 生成 TS + React + electron-vite 骨架。
- 建目录结构、设计系统 `tokens.css`、z-index/布局骨架。
- 三栏空布局：侧边栏 / 中央 / 隐藏右栏。

✅ 验收：`npm run dev` 打开一个空白三栏窗口，无报错。

---

## P1 — 布局与静态界面（不连后端）

**目标**：把截图静态复刻出来。
- 侧边栏：快捷操作 / 任务列表（写死样例数据）/ 用户卡。
- 中央：空态品牌 + 输入框（textarea + 工具栏 + 上下文 + 快捷 chips）。
- 样式完全按 Doc02 的 design tokens。

✅ 验收：界面与截图观感一致；输入框可输入、Enter 发送（本地仅打日志）。

---

## P2 — 后端桥与核心对话链路（主线打通）

**目标**：接入真实后端，实现流式打字机。
- 新增 Python `ws_bridge.py`（Doc03 第 5 节），Electron 主进程拉起并连接。
- 实现主进程 WS 客户端、preload、`useAgentStream`。
- 渲染：`content_delta`/`thinking_delta` → 打字机；`turn_end` → 收尾。
- 断线重连与"重新连接"提示。

✅ 验收：输入一句话，界面上逐字出现后端回复；能收到 usage。

---

## P3 — 工具调用可视化与稳定性

**目标**：补全交互与健壮性。
- `tool_call_start/_delta/tool_call` → 只读工具折叠条。
- 发送中禁用、停止按钮、加载状态。
- Python 崩溃自动重启、WS 异常恢复。
- 会话操作（新建/切换/清空）接入任务列表。

✅ 验收：工具调用可见；连续多轮对话不丢消息、不卡死。

---

## P4 — 目标 / 待办 / 技能等浅接入

**目标**：接入后端剩余能力（只读展示）。
- 目标进度、待办看板、技能列表 通过协议拉取并展示（对应界面预留位）。
- 任务树改为读后端会话列表并支持切换。

✅ 验收：设置弹窗能显示技能/待办；任务树可切换会话。

---

## 后续增量（后端暂无对应能力，逐个补齐）

按"后端接口 → 前端界面"的顺序，每项都先在后端补接口（可能是 Python 或新增模块），再在不破坏现有协议的前提下加 `kind` / 界面。

| 增量 | 说明 | 依赖 |
|------|------|------|
| 模型管理 | 模型下拉真实可选，可配多模型 / provider / key | 后端需暴露模型列表与切换能力（现仅 `.env` 单模型）|
| 对话历史管理 | 历史/搜索/重命名/删除 | 后端 session 元信息接口完善（文件名→标题/时间）|
| 权限控制 | "完全访问"下拉真实生效 | 后端权限校验接口（已有 `check_permission`，需暴露）|
| 输入框「+」加号菜单 | 弹框与两段式菜单项（添加内容/执行方式）**已落地（2026-09-20，外壳）**；六个条目中「添加文件或图片」**已接线（2026-09-20）**，其余五项待逐项确认后接线（接线点 `InputBox.handlePlusPick`） | 逐条定：附件见 Doc12；`@` 引用需路径引用能力；`#` 引用历史会话需历史检索；`/` 命令或 Skill 复用现有 skills 能力；计划/目标模式需会话级模式开关（后端已有 `goal` 能力） |
| 自动化 | 侧边栏「自动化」入口变为真实功能（插件市场 / 模板库 / 办公助理入口已于 2026-09-20 从侧边栏移除） | 后端需提供定时任务配置与调用能力 |
| 账号 / 认证 / 订阅 | 用户卡真实化 | 后端无账号体系，需新增服务或走远端 |

> 每项增量都会先更新 Doc02（UI）与 Doc03（协议）再实现，避免框架设计与落地脱节。

### 已落地增量（回填记录）

| 增量 | 状态 | 说明 |
|------|------|------|
| 模型管理 | ✅ | 模型下拉/设置页（连接中心 v2），见 Doc06 |
| 对话历史管理 | ✅ | 会话列表/重命名/回收站/切会话回放，见 Doc07 |
| **token 消耗记录与显示** | ✅ 2026-09-16 | 每轮 footer 两段式统计（本轮 + 本会话累计 + 缓存命中占比）、圆圈 tooltip 四行（上下文/累计输入/缓存命中/输出）；新增 `usage_stats` 事件；轮级 usage 落 jsonl 末条 assistant 行、会话级 `usage_totals` 落元数据；子智能体（含后台迟到）一并计入；配套模型上下文白名单投影 `_model_messages()`。详见 Doc02 §3.2/§3.3、Doc03 §2.5、Doc07 §2.3.2、Doc08 §6.1 |
| **统计窗口修复 + 轮级模型快照** | ✅ 2026-09-16 | 修复统计误用全局 env `MAX_CONTEXT_TOKENS`（1M）而非所选模型真实窗口（如 128k）的 bug：前端 `resolveOverridesPayload` 只要模型元数据可查就始终显式携带窗口（未选择=标准）；后端缺省回落模型标准窗口（`SessionRuntime._resolve_turn_context`）；切会话 `context_stats` 按会话元数据解析窗口（`ws_bridge._resolve_session_window`，不改共享压缩器状态）。新增轮级 `model_info` 节点（与 usage 平级）落 jsonl、`usage_stats` 事件携带 `model` 字段，footer 第一段括号内显示「模型名 · 窗口 · 思考档位」；已落盘统计数据不回改。详见 Doc02 §3.2、Doc03 §2.3/§2.4/§2.5、Doc07 §2.3.2 |
| **工作空间管理（多项目）** | ✅ 2026-09-18 | 选定目录 = 工具沙箱根 + 一份同构元数据目录（`~/.aigent/projects/<id>/`）；`projects.json` 索引 + `ws` 短码；会话/任务/记忆/团队/工作流按空间隔离，技能/MCP/模型配置/日志保持全局；侧边栏改为**工作空间树**（default 固定第一、可折叠、行内 `+`、右键重命名/删除/在 Finder 打开、会话超 15 条折叠、空间级运行与未读徽标）；输入框 chip 接线为工作空间选择器（两段式下拉 + 选择文件夹）；新增 5 个控制命令与 `projects` 信封；**会话 id 跨空间查重**；引擎层路径由模块级常量改为按空间注入的路径束（`Agent(workspace=…)`，不传 = default = 改造前行为）。详见 Doc11、Doc02 §2.3/§3.3、Doc03 §2.2/§2.7、Doc05 §二、Doc07 §2.1/§2.2/§2.5/§2.6 |
| **输入框「+」添加内容菜单（外壳）** | ✅ 2026-09-20 | 新增 `components/Chat/PlusMenu.tsx` + `chat.css` 的 `.plus-menu*`；两段式（添加内容 4 项 / 执行方式 2 项），数据驱动（`PLUS_MENU_SECTIONS`）、向上弹出、遮罩 + Esc 收起、`activeKey` 预留执行方式勾选位；新增图标 `filePlus`/`at`/`hash`/`slash`/`listTodo`/`target`。各条目功能未接线，点击暂以 toast 占位，接线点 `InputBox.handlePlusPick`。**同日移除原第三段「验收 → 交付」**（语义未定，参考产品也已去掉）。详见 Doc02 §3.3 |
| **会话附件（添加文件或图片）** | ✅ 2026-09-20 | 三条入口（`+` 菜单原生对话框 / 拖拽 / 粘贴截图）全部接线；后端新增 `agents/attachments.py`（`.attachments/` 复制存储 + 草稿区 + pdf/docx/xlsx/pptx 文本抽取 + Pillow 图片缩放 + 草稿/孤儿 GC）；**磁盘只存中性引用块、发送边界才展开为厂商线格式**（引擎层仅 2 处授权改动，且"无附件请求体与改造前逐字节一致"有回归断言）；`attachment_stage` 命令 + `attachments_staged` 信封；`aigent-att://` 自定义协议显示缩略图（按 `(空间, att_id)` 寻址，跨迁移稳定）；**「仅附件无正文」为合法发送**（修掉 `main/index.ts` 里 `!payload.text` 静默丢弃的守卫）；模型图片能力前后端双守卫。后端新增 87 例单测（全量 355 例全绿），前端 headless Chromium A/B 实测 + 视觉快照。详见 Doc12、Doc02 §3.3、Doc03 §2.8/§6、Doc05 §二、Doc06 §三、Doc07 §2.3/§2.6 |

---

## 里程碑产出物

| 阶段 | 产出 |
|------|------|
| P0 | 可运行空白 Electron 窗口 |
| P1 | TraeWork 静态界面复刻 |
| P2 | 真实流式对话跑通 |
| P3 | 工具调用可视化 + 稳定运行 |
| P4 | 后端能力全覆盖（浅层） |

---

## 迭代原则（贯穿始终）

1. **单条链路打穿**：不一次做所有功能，先把"输入→流式→展示"跑通。
2. **后端零改动**：新增能力一律走后端新增薄接口/薄桥，不动 `agent_full_v2.py`。
3. **协议先行**：任何新界面功能先定义 `kind` 与事件类型，前端按协议实现。
4. **可回退**：每阶段独立成提交，坏了能单独回退。

---

## 下一步行动（经你确认后执行）

- [ ] 你审阅 04 篇文档，确认技术选型与分期无异议
- [ ] 确认后，我按 **P0 → P1** 顺序开始搭建工程骨架与静态界面
- [ ] 你同步在 Python 侧审阅 `ws_bridge.py` 薄桥设计，准备 P2 联调

> 回到目录：[00-README.md](00-README.md)